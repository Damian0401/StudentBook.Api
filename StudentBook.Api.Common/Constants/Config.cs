namespace StudentBook.Api.Common.Constants;

public static class Config
{
    public static class ConnectionStrings
    {
        public const string Database = $"{nameof(ConnectionStrings)}:{nameof(Database)}";
    }
}